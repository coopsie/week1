//Create a variable called word that takes a string. Create an if statement that
//checks if the last letter is the same as the first. If it is return true, otherwise return false.

let word = "destined"


if (word[0] == word [7]){
    console.log("true")
}
else {
    console.log("false")
}